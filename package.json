{
  "name": "wikiblank-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Test end-to-end (Playwright) che attraversano frontend, backend e database",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
